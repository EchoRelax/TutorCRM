# PostgreSQL connection string (local dev with docker-compose below)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/lumen

# Secret used to sign session JWTs. Generate one:
#   openssl rand -base64 32
AUTH_SECRET=
